trigger:

- main



pool:

  name: Default



steps:

# Checkout source code

- checkout: self



# Verify files on Windows Agent

- script: |

    echo ===== FILES IN WORKSPACE =====

    dir /s "$(System.DefaultWorkingDirectory)"

  displayName: 'Verify Files'



# Copy files to Ubuntu VM

- task: CopyFilesOverSSH@0

  displayName: 'Copy Website Files to VM'

  inputs:

    sshEndpoint: 'react-vm-ssh'

    sourceFolder: '$(System.DefaultWorkingDirectory)'

    contents: |

      calculater.html

      script.js

      style.css

    targetFolder: '/tmp/website'

    cleanTargetFolder: true

    overwrite: true



# Deploy website on Ubuntu VM

- task: SSH@0

  displayName: 'Deploy Website on Nginx'

  inputs:

    sshEndpoint: 'react-vm-ssh'

    runOptions: 'commands'

    commands: |

      set -e



      echo "===== INSTALL NGINX ====="

      sudo apt-get update

      sudo apt-get install -y nginx



      echo "===== FILES IN /tmp/website ====="

      ls -la /tmp/website



      echo "===== PREPARE WEB DIRECTORY ====="

      sudo mkdir -p /var/www/html

      sudo rm -rf /var/www/html/*



      echo "===== COPY WEBSITE FILES ====="

      sudo cp -r /tmp/website/* /var/www/html/



      echo "===== RENAME HTML FILE ====="

      sudo mv /var/www/html/calculater.html /var/www/html/index.html



      echo "===== SET PERMISSIONS ====="

      sudo chown -R www-data:www-data /var/www/html

      sudo chmod -R 755 /var/www/html



      echo "===== TEST NGINX ====="

      sudo nginx -t



      echo "===== RESTART NGINX ====="

      sudo systemctl enable nginx

      sudo systemctl restart nginx



      echo "===== NGINX STATUS ====="

      sudo systemctl status nginx --no-pager



      echo "===== DEPLOYED FILES ====="

      ls -la /var/www/html

 